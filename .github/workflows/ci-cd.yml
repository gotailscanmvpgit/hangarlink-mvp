# ──────────────────────────────────────────────────────────────────────────────
# HangarLinks CI/CD Pipeline
# ──────────────────────────────────────────────────────────────────────────────
name: CI / CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ─────────────────────────────────────────
  # JOB 1: Continuous Integration (CI)
  # ─────────────────────────────────────────
  ci:
    name: Test and Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install system dependencies (WeasyPrint)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            gcc libpango-1.0-0 libpangoft2-1.0-0 \
            libgdk-pixbuf2.0-0 libcairo2 \
            libffi-dev zlib1g libjpeg-dev \
            libgobject-2.0-0

      - name: Install dependencies
        run: |
          pip install Flask-Limiter
          pip install flask-recaptcha
          pip install -r requirements.txt
          pip install pytest-flask pytest-mock # Ensure common test plugins present

      - name: Run Database Migrations (Validate Schema)
        env:
          FLASK_APP: app.py
          DATABASE_URL: "sqlite:///:memory:"
          SECRET_KEY: ci-testing-key
        run: flask db upgrade

      - name: Validate Jinja2 Templates (Syntax Check)
        env:
          FLASK_APP: app.py
        run: python tests/template_validator.py

      - name: Run Pytest (Unit / Integration)
        env:
          FLASK_APP: app.py
          FLASK_ENV: testing
          DATABASE_URL: "sqlite:///:memory:"
          SECRET_KEY: ci-testing-key
          STRIPE_SECRET_KEY: sk_test_mock
          STRIPE_PUBLISHABLE_KEY: pk_test_mock
        run: pytest tests/tests.py tests/test_listing_detail_crash.py -n auto -v

      - name: Log in to Docker Hub
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/hangarlink:latest
            ${{ secrets.DOCKER_USERNAME }}/hangarlink:${{ github.sha }}

  # ─────────────────────────────────────────
  # JOB 2: Continuous Deployment (CD)
  # ─────────────────────────────────────────
  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    needs: ci  # CD only starts if CI passes (Critical Quality Gate)
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: railway up --service web --detach
